// 自定义配置，参考 ./default/setting.config.js，需要自定义的属性在这里配置即可
module.exports = {
    theme: {
        // color: "#13c2c2",
        color: "#1890ff",
        mode: "dark",
    },
    multiPage: true,
    animate: {
        disabled: true,
        // name: "lightSpeed",
        //name: "slide",
        direction: "default",
    },
};
